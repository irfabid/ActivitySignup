import { Activities.Actions } from './activities.actions';

describe('Activities.Actions', () => {
  it('should create an instance', () => {
    expect(new Activities.Actions()).toBeTruthy();
  });
});
